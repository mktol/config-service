spring:
  application:
    name: configuration-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIGURATION_REPO:https://github.com/mktol/config-repo.git}


server:
  port: 8888
